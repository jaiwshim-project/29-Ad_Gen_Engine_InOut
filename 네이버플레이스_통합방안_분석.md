# 네이버 플레이스 4단계 블록 구조 통합 방안 분석

## 📊 기존 시스템 현황

### 현재 MarketingEngine 구조
```
MarketingEngine (2,223 lines)
├── 5단계 신뢰 구축 시스템 (STAGE 1-5)
├── 조건부 단어풀 시스템 (이미 구현됨)
├── 콘텐츠 생성 메서드
│   ├── generateIntegratedContent() - 통합 콘텐츠
│   ├── generateIdentityStyle() - 정체성 기반
│   ├── generateStorytelling() - 스토리텔링
│   └── generateHookStoryOffer() - HSO 구조
└── 이론적 기반
    ├── 러셀 브런슨 (Hook-Story-Offer, AIDA)
    ├── 간다 마사노리 (마케팅 피라미드)
    ├── NLP 언어 패턴
    └── 5단계 신뢰 구축
```

### 네이버 플레이스 4단계 블록 구조
```
NaverPlaceTrustEngine
├── 4단계 블록 구조 (Block 1-4)
│   ├── Block 1: 공감 시작 (환자 불안 제시)
│   ├── Block 2: 선택 기준 설명 (2-3가지)
│   ├── Block 3: 결론 유보 (판단 유예)
│   └── Block 4: 질문형 종결 (필수)
├── 네이버 플레이스 신호 매핑
│   ├── 체류 시간 → 설명형 구조
│   ├── 공감(좋아요) → 초반 20% 공감 배치
│   ├── 댓글 → 질문형 종결
│   ├── 저장 → 정보 밀도 유지
│   └── 재방문 → 시리즈 콘텐츠
├── 금지 단어 필터링
└── 위반 로그 시스템
```

---

## 🔀 방안 1: 기존 시스템에 통합

### ✅ 장점

#### 1. **단일 진입점 (Single Source of Truth)**
```javascript
// 사용자는 하나의 엔진만 사용
const engine = new MarketingEngine();
const content = engine.generate({
    channel: 'naver_place',  // 채널 지정
    stage: 3,
    topic: '임플란트 치료'
});
```

#### 2. **기존 단어풀 시스템 재사용**
- MarketingEngine의 단어풀(56-200 line)을 그대로 활용
- 중복 코드 최소화
- 유지보수 포인트 단일화

#### 3. **5단계 신뢰 구축과 자연스러운 연결**
```javascript
// STAGE별로 자동으로 적절한 구조 선택
if (context.channel === 'naver_place') {
    return this.generateNaverPlaceContent(context);
} else if (context.channel === 'blog') {
    return this.generateStorytelling(context);
} else {
    return this.generateIntegratedContent(context);
}
```

#### 4. **전체 시스템 일관성 유지**
- 모든 콘텐츠가 동일한 신뢰 철학 기반
- STAGE별 제약이 모든 채널에 일관되게 적용

### ❌ 단점

#### 1. **파일 크기 증가**
- 현재 2,223 lines → 약 3,000+ lines 예상
- 코드 가독성 저하 우려
- 수정/디버깅 시 복잡도 증가

#### 2. **채널별 특화 로직 복잡화**
```javascript
// 조건문이 많아짐
if (channel === 'naver_place') {
    // 4단계 블록 구조
    if (stage <= 2) { /* ... */ }
    else if (stage === 3) { /* ... */ }
} else if (channel === 'blog') {
    // 스토리텔링 구조
} else if (channel === 'landing') {
    // HSO 구조
}
```

#### 3. **테스트 복잡도 증가**
- 단일 클래스에 모든 로직이 집중
- 채널별 독립 테스트 어려움
- 회귀 테스트(regression test) 범위 확대

#### 4. **확장성 제약**
- 새로운 채널 추가 시마다 클래스 수정 필요
- Open-Closed 원칙(OCP) 위배
- 향후 인스타그램, 유튜브 등 추가 시 부담

---

## 🔀 방안 2: 독립 모듈로 구성 (추천 ⭐)

### ✅ 장점

#### 1. **관심사의 분리 (Separation of Concerns)**
```
시스템 구조:
┌─────────────────────────────────┐
│   ContentGeneratorOrchestrator   │  ← 통합 관리자
│     (채널 라우터)                 │
└──────────────┬──────────────────┘
               │
       ┌───────┴────────┬─────────────┬──────────────┐
       │                │             │              │
┌──────▼───────┐ ┌─────▼──────┐ ┌───▼────────┐ ┌──▼─────────┐
│ Marketing    │ │   Naver    │ │  Instagram │ │  YouTube   │
│   Engine     │ │   Place    │ │   Engine   │ │   Engine   │
│  (범용)       │ │  (특화)    │ │  (향후)    │ │  (향후)    │
└──────────────┘ └────────────┘ └────────────┘ └────────────┘
```

#### 2. **채널 특화 최적화**
```javascript
// 네이버 플레이스 전용 최적화
class NaverPlaceTrustEngine {
    // 오직 네이버 플레이스만을 위한 로직
    // - 4단계 블록 구조
    // - 댓글 유도 전략
    // - 네이버 알고리즘 대응
}

// 기존 엔진은 범용적으로 유지
class MarketingEngine {
    // 블로그, 랜딩페이지, 일반 광고 등
}
```

#### 3. **독립적인 테스트 및 배포**
```javascript
// 네이버 플레이스 엔진만 수정해도 됨
// 다른 채널에 영향 없음
describe('NaverPlaceTrustEngine', () => {
    it('항상 질문으로 끝나야 함', () => {
        const content = engine.generate({...});
        expect(content.body[3]).toMatch(/\?$/);
    });
});
```

#### 4. **프로덕트 레벨 확장성**
```javascript
// 새 채널 추가가 기존 코드에 영향 없음
class InstagramReelsEngine { /* ... */ }
class YouTubeShortsEngine { /* ... */ }

// Orchestrator만 수정
orchestrator.register('instagram', InstagramReelsEngine);
orchestrator.register('youtube', YouTubeShortsEngine);
```

#### 5. **전문성 강화**
- 각 채널의 특성에 100% 집중
- 네이버 플레이스 알고리즘 변경 시 독립적 대응
- 채널별 AB 테스트 용이

### ❌ 단점

#### 1. **초기 구현 복잡도**
- Orchestrator 패턴 구현 필요
- 공통 로직 추상화 필요 (Base Class or Mixin)

#### 2. **코드 중복 가능성**
```javascript
// 단어풀 시스템이 중복될 수 있음
// → 해결: 공통 모듈로 분리
class WordPoolManager {
    // 모든 엔진이 공유하는 단어풀
}
```

#### 3. **학습 곡선**
- 개발자가 여러 클래스를 이해해야 함
- 하지만 각 클래스는 단순해져서 실제로는 쉬워질 수 있음

---

## 🎯 **최종 추천: 방안 2 (독립 모듈) + Orchestrator 패턴**

### 이유

#### 1. **장기적 확장성**
- 프랜차이즈 모델에서는 다양한 채널 대응 필수
- 네이버 플레이스 외에도 인스타그램, 유튜브, 카카오톡 채널 등 추가 예정

#### 2. **유지보수성**
- 네이버 알고리즘 변경 시 독립적 대응
- 한 채널의 버그가 다른 채널에 영향 없음

#### 3. **팀 협업 용이성**
- 채널별로 개발자 분업 가능
- 동시 개발/배포 가능

#### 4. **프로덕트 차별화**
- "우리는 각 채널의 특성을 100% 이해하고 최적화한다"
- 이것 자체가 경쟁력

---

## 🏗️ 전체 프로세스 통합 설계

### 아키텍처 다이어그램

```
사용자 입력
    ↓
┌───────────────────────────────────┐
│  진단 시스템                       │
│  - STAGE 판별 (1-5)               │
│  - 점수 산출 (공감/이해/결정)      │
└───────────┬───────────────────────┘
            │
            ↓
┌───────────────────────────────────┐
│  ContentGeneratorOrchestrator     │
│  (채널 라우터)                     │
│                                   │
│  router(channel, context) {       │
│    switch(channel) {              │
│      case 'naver_place':          │
│        return naverEngine.generate│
│      case 'blog':                 │
│        return marketingEngine     │
│              .generateStorytelling│
│      case 'landing':              │
│        return marketingEngine     │
│              .generateHSO         │
│    }                              │
│  }                                │
└───────────┬───────────────────────┘
            │
    ┌───────┴────────┬──────────────────┐
    ↓                ↓                  ↓
┌─────────┐   ┌──────────────┐   ┌──────────────┐
│ Naver   │   │  Marketing   │   │   공통       │
│ Place   │   │   Engine     │   │ WordPool     │
│ Engine  │   │   (범용)     │   │  Manager     │
│ (특화)  │   │              │   │              │
└────┬────┘   └──────┬───────┘   └──────┬───────┘
     │               │                  │
     └───────────────┴──────────────────┘
                     │
                     ↓
            ┌─────────────────┐
            │ 금지 단어 필터링 │
            │ (공통 레이어)    │
            └────────┬─────────┘
                     │
                     ↓
            ┌─────────────────┐
            │  위반 로그 기록  │
            └────────┬─────────┘
                     │
                     ↓
              최종 콘텐츠 출력
```

### 데이터 흐름

```javascript
// 1. 진단 결과 입력
const diagnosisResult = {
    clinic_id: "CLINIC_001",
    stage: 3,
    scores: {
        empathy: 72,
        understanding: 68,
        decision_support: 65
    }
};

// 2. 채널 선택 및 콘텐츠 생성 요청
const orchestrator = new ContentGeneratorOrchestrator();
const content = orchestrator.generate({
    channel: 'naver_place',  // ← 채널 지정
    diagnosis: diagnosisResult,
    topic: '임플란트 상담'
});

// 3. 내부적으로 적절한 엔진 호출
// orchestrator → NaverPlaceTrustEngine
//              → WordPoolManager (단어풀 검증)
//              → ForbiddenWordFilter (금지어 필터)
//              → ViolationLogger (로그 기록)

// 4. 최종 결과
{
    title: "임플란트 치료를 결정하기 전에 가장 많이 고민하시는 부분",
    body: [
        "임플란트를 앞두고 걱정이 생기는 것은 자연스러운 일입니다.",
        "치료 방법을 선택할 때는 과정, 기간, 그리고 본인에게 맞는 방향을 충분히 이해하는 것이 중요합니다.",
        "이 부분은 상황에 따라 다를 수 있기 때문에, 서두르지 않고 생각해보셔도 괜찮습니다.",
        "여러분은 어떤 기준을 가장 중요하게 보고 계신가요?"
    ],
    metadata: {
        channel: 'naver_place',
        stage: 3,
        structure: 'four_block',
        hasQuestionEnding: true,
        violations: []
    }
}
```

---

## 📁 파일 구조 (최종 권장안)

```
C:\01 Claude-Code\00 광고 콘텐츠 생성 AI 플랫폼\
│
├── 📄 marketing-engine.js (기존, 유지)
│   └── 범용 마케팅 콘텐츠 생성 (블로그, 랜딩, 일반 광고)
│
├── 📄 naver-place-trust-engine.js (신규)
│   └── 네이버 플레이스 전용 4단계 블록 구조
│
├── 📄 content-orchestrator.js (신규)
│   └── 채널 라우터 + 통합 관리
│
├── 📄 word-pool-manager.js (신규, 공통 모듈)
│   └── 모든 엔진이 공유하는 단어풀 시스템
│
├── 📄 forbidden-word-filter.js (신규, 공통 모듈)
│   └── 금지 단어 필터링 + 위반 로그
│
└── 📄 trust-message-constants.js (신규)
    └── 프랜차이즈 통일 메시지 관리
```

---

## 🚀 구현 단계별 로드맵

### Phase 1: 공통 모듈 분리 (Week 1)
```
✅ 1-1. WordPoolManager 클래스 생성
✅ 1-2. ForbiddenWordFilter 클래스 생성
✅ 1-3. 기존 MarketingEngine에서 공통 부분 추출
```

### Phase 2: 네이버 플레이스 엔진 구현 (Week 2)
```
✅ 2-1. NaverPlaceTrustEngine 클래스 구현
✅ 2-2. 4단계 블록 구조 로직 구현
✅ 2-3. 네이버 플레이스 신호 매핑 구현
✅ 2-4. 단위 테스트 작성
```

### Phase 3: Orchestrator 구현 (Week 3)
```
□ 3-1. ContentGeneratorOrchestrator 클래스 구현
□ 3-2. 채널 라우팅 로직 구현
□ 3-3. 기존 MarketingEngine 통합
□ 3-4. 통합 테스트 작성
```

### Phase 4: UI 통합 및 배포 (Week 4)
```
□ 4-1. input.html 수정 (채널 선택 드롭다운 추가)
□ 4-2. result.html 수정 (채널별 결과 표시)
□ 4-3. 프랜차이즈 가이드라인 문서 작성
□ 4-4. 사용자 테스트 및 피드백
```

---

## 💡 핵심 구현 포인트

### 1. **공통 단어풀 관리**

기존 MarketingEngine의 단어풀을 독립 모듈로 추출:

```javascript
class WordPoolManager {
    constructor() {
        this.initializeWordPools();
    }

    // 기존 marketing-engine.js의 56-200 line 코드 이동
    initializeWordPools() { /* ... */ }

    isWordAllowedForStage(word, stage, scores) { /* ... */ }
}

// 사용
const wordPool = new WordPoolManager();
const naverEngine = new NaverPlaceTrustEngine(wordPool);
const marketingEngine = new MarketingEngine(wordPool);
```

### 2. **채널별 특화 vs 공통 로직 구분**

| 로직 유형 | 위치 | 예시 |
|----------|------|------|
| 채널 공통 | WordPoolManager | STAGE별 단어 허용/금지 |
| 채널 공통 | ForbiddenWordFilter | 금지어 필터링 |
| 네이버 플레이스 전용 | NaverPlaceTrustEngine | 4단계 블록 구조, 질문 종결 |
| 범용 마케팅 | MarketingEngine | HSO, AIDA, 스토리텔링 |

### 3. **확장 가능한 Orchestrator**

```javascript
class ContentGeneratorOrchestrator {
    constructor() {
        this.engines = new Map();
        this.wordPool = new WordPoolManager();
        this.filter = new ForbiddenWordFilter();

        // 엔진 등록
        this.registerEngine('naver_place', new NaverPlaceTrustEngine(this.wordPool));
        this.registerEngine('blog', new MarketingEngine(this.wordPool));
    }

    registerEngine(channel, engine) {
        this.engines.set(channel, engine);
    }

    generate(request) {
        const { channel, diagnosis, topic } = request;
        const engine = this.engines.get(channel);

        if (!engine) {
            throw new Error(`Unknown channel: ${channel}`);
        }

        // 콘텐츠 생성
        let content = engine.generate({ diagnosis, topic });

        // 공통 필터링
        content = this.filter.apply(content, diagnosis.stage);

        return content;
    }
}
```

---

## 🎬 사용 예시

### Case 1: 네이버 플레이스 콘텐츠 생성

```javascript
const orchestrator = new ContentGeneratorOrchestrator();

const content = orchestrator.generate({
    channel: 'naver_place',
    diagnosis: {
        stage: 3,
        scores: { empathy: 72, understanding: 68, decision_support: 65 }
    },
    topic: '임플란트 상담'
});

// 결과: 4단계 블록 구조 + 질문형 종결
console.log(content.body[3]); // "여러분은 어떤 기준을 가장 중요하게 보고 계신가요?"
```

### Case 2: 블로그 콘텐츠 생성

```javascript
const content = orchestrator.generate({
    channel: 'blog',
    diagnosis: {
        stage: 4,
        scores: { empathy: 80, understanding: 75, decision_support: 70 }
    },
    topic: '치아 교정의 올바른 이해'
});

// 결과: 스토리텔링 구조 (기존 MarketingEngine 활용)
```

### Case 3: 랜딩 페이지 콘텐츠 생성

```javascript
const content = orchestrator.generate({
    channel: 'landing',
    diagnosis: {
        stage: 2,
        scores: { empathy: 65, understanding: 60, decision_support: 55 }
    },
    topic: '첫 방문 환자를 위한 안내'
});

// 결과: Hook-Story-Offer 구조
```

---

## 📊 성과 측정 지표

### 네이버 플레이스 전용 지표

| 지표 | 목표 | 측정 방법 |
|-----|------|----------|
| 질문형 종결 비율 | 100% | 자동 검증 |
| 금지어 필터링 성공률 | 100% | 위반 로그 0건 |
| 댓글 발생률 | +30% | 기존 대비 |
| 체류 시간 | +25% | GA 분석 |
| 저장(북마크) 비율 | +20% | 네이버 플레이스 인사이트 |

---

## 🎯 결론 및 권장사항

### ✅ 최종 권장: **독립 모듈 + Orchestrator 패턴**

**이유:**
1. ✅ 장기적 확장성 (인스타그램, 유튜브 등 추가 용이)
2. ✅ 채널별 최적화 극대화
3. ✅ 유지보수 부담 감소
4. ✅ 팀 협업 및 동시 개발 가능
5. ✅ 프로덕트 차별화 강화

**단기적 단점:**
- 초기 구현 복잡도 증가 (Orchestrator 패턴 구현)
- 하지만 이는 1주일 내 해결 가능한 수준

**장기적 이득:**
- 3개월 후 새 채널 추가 시 기존 코드 수정 불필요
- 채널별 독립적 AB 테스트 가능
- 프랜차이즈 모델에 최적화된 구조

---

## 📝 Next Actions

1. **즉시 실행:**
   - `content-orchestrator.js` 기본 구조 구현
   - `word-pool-manager.js` 공통 모듈 분리

2. **이번 주 완료:**
   - `naver-place-trust-engine.js` 완성
   - 단위 테스트 작성

3. **다음 주:**
   - UI 통합 (채널 선택 드롭다운)
   - 프랜차이즈 가이드라인 문서 작성

---

**문서 버전:** 1.0
**작성일:** 2026-01-13
**작성자:** Claude Code
**검토 필요:** 대표님 최종 승인
